# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# ---- Added for GitHub upload ----

# Firebase deploy cache / local emulator artifacts
.firebase/

# Cloud Functions (Node) dependencies (root /node_modules rule doesn't cover this)
functions/node_modules/

# Local design/backup snapshots (large duplicate site copies)
_design_backup_*/

# ---- Excluded from GitHub upload (per request) ----

# Bundled Python/fontTools tooling residue (702 files)
.codex-tmp/

# Project-local assistant settings
.claude/

# All image / branding assets — logos, marketing/hero imagery, favicons, course photos
*.png
*.jpg
*.jpeg
*.webp
*.gif
*.ico
*.svg

# ---- Withheld to prevent a full/turnkey clone (keystone + core IP) ----
# Firebase connection wiring (nothing connects / build breaks without it)
src/lib/firebase.ts
# Gemini AI integration (rate-limited backend logic)
src/lib/ai/gemini.ts

# Assistant tooling files (not part of the project)
CLAUDE.md
AGENTS.md

# Internal planning / setup docs (withheld under gaps plan)
/docs/

# Backend config (security rules / project wiring — withheld under gaps plan)
firestore.rules
firebase.json
.firebaserc
firestore.indexes.json
